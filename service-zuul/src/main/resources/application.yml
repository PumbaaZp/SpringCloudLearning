eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
server:
  port: 8086
spring:
  application:
    name: service-zuul
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: SERVICE-RIBBON
    api-b:
      path: /api-b/**
      serviceId: SERVICE-FEIGN