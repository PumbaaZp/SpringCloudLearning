#server:  端口未找到修改方式
  #port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PumbaaZp/SpringCloudLearning/
          search-paths: config
      label: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/